SELECT pg_relation_filenode('split_obj_mgmt.size_probe'::regclass) IS NOT NULL AS relation_filenode_known;
SELECT pg_relation_filepath('split_obj_mgmt.size_probe'::regclass) LIKE 'base/%' AS relation_filepath_base;
SELECT pg_filenode_relation(0, pg_relation_filenode('split_obj_mgmt.size_probe'::regclass)) = 'split_obj_mgmt.size_probe'::regclass AS filenode_round_trip;
SELECT pg_relation_filenode('split_obj_mgmt.size_probe_view'::regclass) IS NULL AS view_has_no_filenode;
------------------------------------------------------------------------------------------
[METADATA] SELECT pg_relation_filenode('split_obj_mgmt.size_probe'::regclass) IS NOT NULL AS relation_filenode_known;
----------
[METADATA] SELECT pg_relation_filepath('split_obj_mgmt.size_probe'::regclass) LIKE 'base/%' AS relation_filepath_base;
----------
[METADATA] SELECT pg_filenode_relation(0, pg_relation_filenode('split_obj_mgmt.size_probe'::regclass)) = 'split_obj_mgmt.size_probe'::regclass AS filenode_round_trip;
----------
[METADATA] SELECT pg_relation_filenode('split_obj_mgmt.size_probe_view'::regclass) IS NULL AS view_has_no_filenode;
