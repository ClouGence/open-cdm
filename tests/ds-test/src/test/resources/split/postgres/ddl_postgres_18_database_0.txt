DROP DATABASE IF EXISTS split_db18_builtin WITH (FORCE);
DROP DATABASE IF EXISTS split_db18_icu WITH (FORCE);
CREATE DATABASE split_db18_builtin WITH TEMPLATE = template0 ENCODING = 'UTF8' STRATEGY = WAL_LOG LOCALE_PROVIDER = builtin BUILTIN_LOCALE = 'PG_UNICODE_FAST' ALLOW_CONNECTIONS = true CONNECTION LIMIT = -1 IS_TEMPLATE = false;
DROP DATABASE IF EXISTS split_db18_builtin WITH (FORCE);
CREATE DATABASE split_db18_icu WITH TEMPLATE template0 STRATEGY FILE_COPY LOCALE_PROVIDER icu ICU_LOCALE 'und-x-icu' ICU_RULES '&V << w <<< W';
DROP DATABASE IF EXISTS split_db18_icu WITH (FORCE);
------------------------------------------------------------------------------------------
[DROP_CATALOG] DROP DATABASE IF EXISTS split_db18_builtin WITH (FORCE);
----------
[DROP_CATALOG] DROP DATABASE IF EXISTS split_db18_icu WITH (FORCE);
----------
[CREATE_CATALOG] CREATE DATABASE split_db18_builtin WITH TEMPLATE = template0 ENCODING = 'UTF8' STRATEGY = WAL_LOG LOCALE_PROVIDER = builtin BUILTIN_LOCALE = 'PG_UNICODE_FAST' ALLOW_CONNECTIONS = true CONNECTION LIMIT = -1 IS_TEMPLATE = false;
----------
[DROP_CATALOG] DROP DATABASE IF EXISTS split_db18_builtin WITH (FORCE);
----------
[CREATE_CATALOG] CREATE DATABASE split_db18_icu WITH TEMPLATE template0 STRATEGY FILE_COPY LOCALE_PROVIDER icu ICU_LOCALE 'und-x-icu' ICU_RULES '&V << w <<< W';
----------
[DROP_CATALOG] DROP DATABASE IF EXISTS split_db18_icu WITH (FORCE);
