SELECT * FROM pg_split_walfile_name(NULL);
SELECT segment_number, timeline_id FROM pg_split_walfile_name('000000010000000100000000');
SELECT segment_number, timeline_id FROM pg_split_walfile_name('ffffffFF00000001000000af');
SELECT file_name, file_offset, segment_number
FROM pg_walfile_name_offset('0/0'::pg_lsn + pg_size_bytes(current_setting('wal_segment_size'))),
     pg_split_walfile_name(file_name);
SELECT file_name, file_offset, segment_number
FROM pg_walfile_name_offset('0/0'::pg_lsn + pg_size_bytes(current_setting('wal_segment_size')) + 1),
     pg_split_walfile_name(file_name);
SELECT file_name, file_offset, segment_number
FROM pg_walfile_name_offset('0/0'::pg_lsn + pg_size_bytes(current_setting('wal_segment_size')) - 1),
     pg_split_walfile_name(file_name);
------------------------------------------------------------------------------------------
[SELECT] SELECT * FROM pg_split_walfile_name(NULL);
----------
[SELECT] SELECT segment_number, timeline_id FROM pg_split_walfile_name('000000010000000100000000');
----------
[SELECT] SELECT segment_number, timeline_id FROM pg_split_walfile_name('ffffffFF00000001000000af');
----------
[SELECT|LOG_READ] SELECT file_name, file_offset, segment_number
FROM pg_walfile_name_offset('0/0'::pg_lsn + pg_size_bytes(current_setting('wal_segment_size'))),
     pg_split_walfile_name(file_name);
----------
[SELECT|LOG_READ] SELECT file_name, file_offset, segment_number
FROM pg_walfile_name_offset('0/0'::pg_lsn + pg_size_bytes(current_setting('wal_segment_size')) + 1),
     pg_split_walfile_name(file_name);
----------
[SELECT|LOG_READ] SELECT file_name, file_offset, segment_number
FROM pg_walfile_name_offset('0/0'::pg_lsn + pg_size_bytes(current_setting('wal_segment_size')) - 1),
     pg_split_walfile_name(file_name);
