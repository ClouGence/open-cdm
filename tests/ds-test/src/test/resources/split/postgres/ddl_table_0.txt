CREATE TABLE colors (
	id INT GENERATED ALWAYS AS IDENTITY (INCREMENT BY 3 START WITH 7),
	name VARCHAR(10)
);
CREATE TABLE colors (
	id INT GENERATED BY DEFAULT AS IDENTITY (INCREMENT BY 3 START WITH 7),
	name VARCHAR(10)
);
CREATE TABLE live_measures_copy (uuid, project_uuid, component_uuid, metric_uuid, value, text_value, variation, measure_data, update_marker, created_at, updated_at) AS (SELECT uuid, project_uuid, component_uuid, CAST (metric_id AS VARCHAR (40)) AS metric_uuid, value, text_value, variation, measure_data, update_marker, created_at, updated_at FROM live_measures);
CREATE TABLE issue_changes_copy (
    uuid, kee,
    issue_key,
    user_login,
    change_type,
    change_data,
    created_at,
    updated_at,
    issue_change_creation_date
)
AS (
    SELECT CAST (id AS VARCHAR (40)) AS uuid, kee, issue_key, user_login, change_type, change_data, created_at, updated_at, issue_change_creation_date FROM issue_changes);
CREATE TABLE orders (
    order_id     bigint not null,
    cust_id      bigint not null,
    status       text
) PARTITION BY HASH (order_id);

alter table if exists tcweatherdata
add column predictiontime timestamp;

create table s_2c3e24b36890b0d31aeca286e84c6471 as (
    SELECT DISTINCT ON (sn) sn, num01 FROM "s_f73e29e1c9830041f1bdfa8_2c3e24b36890b0d31aeca286e84c6471"
 );

alter table ACT_RU_INTEGRATION
    add constraint ACT_FK_INT_EXECUTION
        foreign key (EXECUTION_ID_)
            references ACT_RU_EXECUTION (ID_)
        on delete cascade;

CREATE TABLE A1(
   a_id  integer  NOT NULL ,
   b_id  integer  NOT NULL ,
   c_id  integer  NOT NULL ,
   sales_amount  numeric(10, 2)  NOT NULL ,
   order_date  date  NOT NULL ,
   q1  varchar(255) COLLATE "pg_catalog"."default" NULL ,
   q2  varchar(255) COLLATE "pg_catalog"."default" NULL ,
 CONSTRAINT "A1_pkey" PRIMARY KEY ( a_id ) NOT DEFERRABLE )TABLESPACE pg_default;

CREATE INDEX idx_b_id_for_A_1231377909 ON A1 USING btree(b_id "pg_catalog"."int4_ops" asc NULLS LAST) ;
CREATE INDEX idx_c_id_for_A_1509405683 ON A1 USING btree(c_id "pg_catalog"."int4_ops" asc NULLS LAST) ;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE colors (
	id INT GENERATED ALWAYS AS IDENTITY (INCREMENT BY 3 START WITH 7),
	name VARCHAR(10)
);
----------
[CREATE_TABLE] CREATE TABLE colors (
	id INT GENERATED BY DEFAULT AS IDENTITY (INCREMENT BY 3 START WITH 7),
	name VARCHAR(10)
);
----------
[CREATE_TABLE|SELECT] CREATE TABLE live_measures_copy (uuid, project_uuid, component_uuid, metric_uuid, value, text_value, variation, measure_data, update_marker, created_at, updated_at) AS (SELECT uuid, project_uuid, component_uuid, CAST (metric_id AS VARCHAR (40)) AS metric_uuid, value, text_value, variation, measure_data, update_marker, created_at, updated_at FROM live_measures);
----------
[CREATE_TABLE|SELECT] CREATE TABLE issue_changes_copy (
    uuid, kee,
    issue_key,
    user_login,
    change_type,
    change_data,
    created_at,
    updated_at,
    issue_change_creation_date
)
AS (
    SELECT CAST (id AS VARCHAR (40)) AS uuid, kee, issue_key, user_login, change_type, change_data, created_at, updated_at, issue_change_creation_date FROM issue_changes);
----------
[CREATE_TABLE] CREATE TABLE orders (
    order_id     bigint not null,
    cust_id      bigint not null,
    status       text
) PARTITION BY HASH (order_id);
----------
[ALTER_TABLE|ADD_COLUMN] alter table if exists tcweatherdata
add column predictiontime timestamp;
----------
[CREATE_TABLE|SELECT] create table s_2c3e24b36890b0d31aeca286e84c6471 as (
    SELECT DISTINCT ON (sn) sn, num01 FROM "s_f73e29e1c9830041f1bdfa8_2c3e24b36890b0d31aeca286e84c6471"
 );
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table ACT_RU_INTEGRATION
    add constraint ACT_FK_INT_EXECUTION
        foreign key (EXECUTION_ID_)
            references ACT_RU_EXECUTION (ID_)
        on delete cascade;
----------
[CREATE_TABLE] CREATE TABLE A1(
   a_id  integer  NOT NULL ,
   b_id  integer  NOT NULL ,
   c_id  integer  NOT NULL ,
   sales_amount  numeric(10, 2)  NOT NULL ,
   order_date  date  NOT NULL ,
   q1  varchar(255) COLLATE "pg_catalog"."default" NULL ,
   q2  varchar(255) COLLATE "pg_catalog"."default" NULL ,
 CONSTRAINT "A1_pkey" PRIMARY KEY ( a_id ) NOT DEFERRABLE )TABLESPACE pg_default;
----------
[ADD_INDEX] CREATE INDEX idx_b_id_for_A_1231377909 ON A1 USING btree(b_id "pg_catalog"."int4_ops" asc NULLS LAST) ;
----------
[ADD_INDEX] CREATE INDEX idx_c_id_for_A_1509405683 ON A1 USING btree(c_id "pg_catalog"."int4_ops" asc NULLS LAST) ;