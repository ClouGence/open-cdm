SELECT unicode_version();
SELECT unicode_version() IS NOT NULL AS unicode_version_available;
SELECT icu_unicode_version();
SELECT icu_unicode_version() IS NULL OR icu_unicode_version() ~ '^[0-9]+' AS icu_unicode_version_shape;
------------------------------------------------------------------------------------------
[SELECT] SELECT unicode_version();
----------
[SELECT] SELECT unicode_version() IS NOT NULL AS unicode_version_available;
----------
[SELECT] SELECT icu_unicode_version();
----------
[SELECT] SELECT icu_unicode_version() IS NULL OR icu_unicode_version() ~ '^[0-9]+' AS icu_unicode_version_shape;
