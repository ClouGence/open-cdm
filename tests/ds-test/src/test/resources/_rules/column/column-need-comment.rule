if
  @domain.sqlType in ['CREATE_TABLE', 'ADD_COLUMN']
then
  return @func.string.isNotBlank(@domain.comment)
end

if
  @domain.sqlType == 'ALTER_COLUMN' and
  @domain.comment != null
then
  return @func.string.isNotBlank(@domain.comment)
end