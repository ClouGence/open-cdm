interface:
  display_name: "Open CDM Prepare Release"
  short_description: "Create release notes or update version references"
  default_prompt: "Use $open-cdm-change-version to create bilingual release notes, update version references, or prepare the complete open-cdm TARGET_VERSION release, following the scope I request."
